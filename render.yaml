services:
  - type: web
    name: medsave-ai
    env: python

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: >
      streamlit run main.py
      --server.port=$PORT
      --server.address=0.0.0.0